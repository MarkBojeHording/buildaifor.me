{
  "buildCommand": "cd website && rm -rf node_modules package-lock.json && npm install --platform=linux --arch=x64 && npm run build",
  "outputDirectory": "website/dist",
  "env": {
    "NODE_ENV": "production"
  }
}
