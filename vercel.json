{
  "buildCommand": "cd website && export ROLLUP_SKIP_NATIVE=true && export NODE_ENV=production && rm -rf node_modules package-lock.json && npm install && npm run build",
  "outputDirectory": "website/dist",
  "installCommand": "cd website && npm install",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production",
    "ROLLUP_SKIP_NATIVE": "true"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
