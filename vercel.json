{
  "buildCommand": "cd website && export ROLLUP_SKIP_NATIVE=true && export ESBUILD_BINARY_PATH= && export NODE_ENV=production && export PATH=$PATH:./node_modules/.bin && rm -rf node_modules package-lock.json && npm install --no-optional && npm run build",
  "outputDirectory": "website/dist",
  "installCommand": "cd website && npm install --no-optional",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production",
    "ROLLUP_SKIP_NATIVE": "true",
    "ESBUILD_BINARY_PATH": ""
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
