{
  "buildCommand": "cd website && npm install --no-optional && npm run build",
  "outputDirectory": "website/dist",
  "installCommand": "cd website && npm install --no-optional",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
