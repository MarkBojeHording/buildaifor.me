{
  "name": "buildaifor-me-business",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "website"
  ],
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "start": "cd website && npx serve -s dist -l $PORT"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
